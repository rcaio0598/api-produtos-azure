# Perfil de produção (Azure): use SPRING_PROFILES_ACTIVE=prod no App Service
# H2 em arquivo para persistência (dados sobrevivem ao reinício do app)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:h2:file:./data/produtosdb}
spring.h2.console.enabled=false
